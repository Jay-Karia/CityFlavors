generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Category {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  description String
  name        String
  products    String[]
}

model Product {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  category    String @db.ObjectId
  description String
  image       String
  name        String
  price       Float
}

model User {
  id       String  @id @default(auto()) @map("_id") @db.ObjectId
  address  String  @db.ObjectId
  email    String  @unique
  isAdmin  Boolean
  name     String
  password String
}

model UserInfo {
  id      String @id @default(auto()) @map("_id") @db.ObjectId
  address String
  city    String
  pincode String
  userId  String @unique @db.ObjectId
}
